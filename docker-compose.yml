version: "3.9"

services:
  mongo:
    image: mongo:7
    container_name: thalassia-mongo
    ports:
      - "12345:27017"   
    volumes:
      - mongo_data:/data/db
    environment:
      - MONGO_INITDB_DATABASE=thalassia

volumes:
  mongo_data:
